import contact from './contact.yml';
import sendMessage from './send-message.yml';
import createAccount from './create-account.yml';
import createCampaign from './create-campaign.yml';
import leaveFeedback from './leave-feedback.yml';
import productPurchase from './product-purchase.yml';

const schema = {
  contact,
  sendMessage,
  createAccount,
  createCampaign,
  leaveFeedback,
  productPurchase,
};

export default schema;

